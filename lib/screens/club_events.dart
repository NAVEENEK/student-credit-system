import 'package:flutter/material.dart';

class ClubEvents extends StatelessWidget {
  const ClubEvents({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('this club events'),),
    );
  }
}